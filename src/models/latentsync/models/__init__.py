"""LatentSync model architecture — UNet3DConditionModel and supporting blocks.

These files are sourced from ByteDance/LatentSync (Apache 2.0) and adapted as
first-party SwiftDub code. Training-only utilities have been removed.

Run `scripts/vendor/migrate_models.py` to automatically copy and patch
model architecture files from models/vendor/latentsync into this package.
"""
